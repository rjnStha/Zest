import { ScrollJumpDirective } from './scroll-jump.directive';

describe('ScrollJumpDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollJumpDirective();
    expect(directive).toBeTruthy();
  });
});
